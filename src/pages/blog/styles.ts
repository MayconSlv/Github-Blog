import styled from "styled-components";

export const PostListContainer = styled.section`
  width: 100%;
  display: grid;
  gap: 3.2rem;
  grid-template-columns: repeat(auto-fit, minmax(41.6rem, 1fr));
  margin: 4.8rem 0 23rem;
`